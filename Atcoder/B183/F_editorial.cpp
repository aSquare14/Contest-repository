#include <bits/stdc++.h>
#define ll long long
const unsigned int mod = 1000000007;
using namespace std;
int main() {
    int n; cin>>n;
    int q; cin>>q;
    vector<int> arr(n,0);
    for(int i=0;i<n;i++) {
        int c; cin>>c;
       
    }
    for(int i=0;i<q;i++)
    {
        int query; cin>>query;
        if(query==1) {
            int a,b;
            cin>>a>>b;
        }
        else if(query==2) {
            int clss,student;
            cin>>student>>clss;
        }
    }
    return 0;
}